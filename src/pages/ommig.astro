---
import "../styles/style.css";
import Mainlayout from "../components/layouts/Mainlayout.astro";
import FlowerAnimation from "../components/FlowerAnimation.astro";
import ScrollArrow from "../components/ScrollArrow.astro";
---

<Mainlayout title="Om mig">
    <div class="header">
      <div class="progress-container">
        <div class="progress-bar" id="myBar"></div>
      </div>
    </div>
    <button class="go-back-button" onclick="goBackLeft()">Go Back Left</button>
    <div id="container">
      <div class="box one">
          <div class="intro_text">
              <h1>Kunstneren bag<br>lærredet</h1>
              <div id="ScrollArrow_container">
              <ScrollArrow></ScrollArrow>
              </div>
          </div>
          <img id="susse_half" src="/images/susse_eyes.webp" alt="susse half face">  
      </div>
      <div class="box_large two">
          <div class="max_width_text first">
              <p>Jeg er Susse Volander, en kunstner, der lever og ånder for at udtrykke mig kreativt!</p>
          </div>
          <div class="max_width_text second">
              <p> Min rejse ind i kunstens verden begyndte, da jeg som 7-årig tegnede løs hos min farmor. Hun købte mig min første kasse med tegnekul og papir i en boghandel i Aarhus, og siden da har jeg været solgt til kunstens magiske univers.</p>
          </div>
      </div>
      <div class="box three">
          <div id="tema_text">
              <h1>Genbrug og kreativitet</h1>
              <hr id="tema_line">
              <p>Min kunstneriske stil har altid en rød tråd, der binder det hele sammen. Jeg maler primært for mig selv og finder inspiration i de genbrugsmaterialer, som andre har kasseret. Et legetøjsgris uden ben eller et puslespil med manglende brikker kan give mig nye idéer og liv til mine værker. </p>
              <p>Når det kommer til kreative blokeringer, er jeg heldig ikke at opleve dem, da der er så mange ideer der kører i mit hoved der bare skal UD. Jeg hører tit folk sige, at de hader mandage, fordi de skal tilbage på arbejde; for mig er det helt modsat. Jeg elsker mit arbejde så meget, at jeg glæder mig til mandag morgen, hvor jeg kan male igen!</p>
              <img id="hanging_art" src="/images/hanging_art_collage.webp" alt="hanging art">
              <img id="pipaluk_hanging" src="/images/webpp/pipaluk_hanging_around.webp" alt="hanging pipaluk">
              <img id="hoved_uden_piner" src="/images/hoved_uden_piner.webp" alt="hanging hoved uden piner">
            </div>
      </div>
      <div class="box three"></div>
      <div class="box four"></div>
    </div>
    
  <script>
      document.getElementById('container').onscroll = function() { 
          myFunction(); 
          fadeBackground();
      };
  
      function myFunction() {
          var container = document.getElementById('container');
          var winScroll = container.scrollLeft;
          var width = container.scrollWidth - container.clientWidth;
          var scrolled = (winScroll / width) * 100;
          document.getElementById("myBar").style.width = scrolled + "%";
      }
  
      function fadeBackground() {
          var container = document.getElementById('container');
          var winScroll = container.scrollLeft;
  
          var fadeElement = document.querySelector('.box_large.two');
          var nextElement = document.querySelector('.box_large.three');
          var fadeStart = fadeElement.offsetLeft + (fadeElement.clientWidth * 0.25); // Start fading earlier
          var fadeEnd = fadeElement.offsetLeft + (fadeElement.clientWidth * 0.75); // End fading earlier
  
          if (winScroll >= fadeStart && winScroll <= fadeEnd) {
              var scrolled = (winScroll - fadeStart) / (fadeEnd - fadeStart);
              var opacity = Math.max(0, 1 - scrolled);
              fadeElement.style.opacity = opacity;
          } else if (winScroll < fadeStart) {
              fadeElement.style.opacity = 1;
          } else if (winScroll > fadeEnd) {
              fadeElement.style.opacity = 0;
          }
      }
  
      function goBackLeft() {
          var container = document.getElementById('container');
          container.scrollLeft = 0;
      }
  </script>
  </Mainlayout>


<style>
 /* CSS */
body {
  margin: 0;
  overflow: hidden; /* Prevent vertical scroll */
}

.box, .box_large {
  width: 100vw;
  height: 90vh;
  display: inline-block;
  position: relative;
}

.box_large {
    width: 2400px;
}

.box div, .box_large div {
  width: 100vw;
  height: 100vh;
  font-size: 96px;
  color: #FFF;
  position: absolute;
  font-weight: bold;
}

.box_large div {
    height: auto;
    background-color: rgba(255, 255, 255, 0.87); /* White with 87% opacity */
    padding: 20px; /* Optional: Add padding for spacing inside the box */
    border-radius: 10px; /* Optional: Add rounded corners */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Optional: Add a subtle shadow */
}

.box_large.two, .box_large.three {
  /* Set a specific height */
  min-height: 500px;

  /* Create the parallax scrolling effect */
  background-attachment: fixed;
  background-position: center;
  background-repeat: no-repeat;
  background-size: cover;
  background-image: url("/images/webpp/Susse_garden.webp");
}

.box_large.three, .box.three {
 background-image: none;
 width: 2500px;
}

#container {
  overflow-x: scroll;
  overflow-y: hidden;
  white-space: nowrap;
}

.one, .two, .three, .four { background-color: #FCFFF6; }

.header {
  position: fixed;
  top: 0;
  z-index: 1;
  width: 100%;
  background-color: #f1f1f1;
}

.progress-container {
  width: 100%;
  height: 8px;
  background: #ccc;
}

.progress-bar {
  height: 8px;
  background: #04AA6D;
  width: 0%;
}

.go-back-button {
  position: fixed;
  bottom: 50px;
  right: 20px;
  padding: 10px 20px;
  background-color: #04AA6D;
  color: white;
  border: none;
  cursor: pointer;
  font-size: 16px;
  z-index: 100;
}

.go-back-button:hover {
  background-color: #028a5e;
}

/* Intro Section */
#intro img {
  position: absolute;
  top: 40%;
  right: 0;
  transform: translateY(-40%);
  width: auto;
  opacity: 0;
  z-index: 1; /* Ensure the intro image is above the parallax image */
}

.intro_text h1 {
  line-height: 50px;
  margin: 20px;
  opacity: 0; /* Initially set opacity to 0 to hide the text */
  animation: fadeIn 1s ease forwards; /* Apply the fadeIn animation */
}

#susse_half {
  position: absolute;
  bottom: 0;
  right: 0;
  max-height: 100%; /* Ensure image doesn't exceed the height of its container */
  height: 80%;
  object-fit: contain; /* Maintain aspect ratio without stretching */
  opacity: 0; /* Initially set opacity to 0 to hide the image */
  animation: fadeIn 1s ease forwards; /* Apply the fadeIn animation */
}

#ScrollArrow_container {
  margin-top: -90px;
  margin-left: 10px;
  opacity: 0; /* Initially set opacity to 0 to hide the scroll arrow */
  animation: fadeIn 1.5s ease forwards 1s; /* Apply the fadeIn animation with a delay of 0.5s */
}

@keyframes fadeIn {
  from {
    opacity: 0; /* Start with opacity 0 */
  }
  to {
    opacity: 1; /* Fade in to opacity 1 */
  }
}

.max_width_text {
  line-height: normal;
  white-space: wrap;
  margin: 10px;
  max-width: 400px;
  background-color: white;
}

.first {
  margin-left: 20%;
  margin-top: 4%;
}

.second {
  margin-left: 35%;
  margin-top: 16%;
}

#tema_text {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  margin-top: 30px;
  margin-left: 600px;
  position: absolute;
}

#tema_text h1 {
  text-align: right;
}

#tema_text p {
  max-width: 350px;
  white-space: wrap;
  margin-top: 15px;
}

#tema_line {
  width: 1200px;
  height: 2px;
  color: #F20056;
  margin: 10px 0; /* Space between line and text */
}
@keyframes swing {
  0% {
    transform: rotate(0deg);
    transform-origin: top center; /* Set the rotation pivot point at the top center */
  }
  50% {
    transform: rotate(5deg);
    transform-origin: top center; /* Maintain the pivot point */
  }
  100% {
    transform: rotate(0deg);
    transform-origin: top center; /* Maintain the pivot point */
  }
}

#hanging_art, #pipaluk_hanging {
  position: absolute;
  top: 65px;
  left: 53%;
  width: 250px;
  height: auto;
  animation: swing 3s infinite; /* Apply the swing animation */
}

#pipaluk_hanging{
    width: 120px;
    top: 55px;
    left: 40%;
}


/* Media query for smaller screens */
@media screen and (max-width: 1224px) {
    #hanging_art {
        left: 270px;
    }
    #pipaluk_hanging{
        left: 70px;
}

}

@media screen and (max-width: 898px) {
    #hanging_art {
        left: 100px; /* Adjust based on viewport width */
    }
    #pipaluk_hanging{
        left: -150px;
}
    
}

@media screen and (max-width: 680px) {
    #hanging_art {
        left: -150px; /* Adjust based on viewport width */
    }
    #pipaluk_hanging{
        left: -350px;
}
}

  </style>
